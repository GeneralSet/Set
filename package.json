{
  "scripts": {
    "preserve": "cargo build --target wasm32-unknown-unknown && wasm-bindgen target/wasm32-unknown-unknown/release/set.wasm --out-dir pkg/ --typescript",
    "serve": "cd demo && webpack-dev-server",
    "pretest": "cargo build --target wasm32-unknown-unknown && wasm-bindgen target/wasm32-unknown-unknown/release/set.wasm --out-dir tests/build/ --nodejs",
    "test": "cd tests && yarn test"
  }
}
