{
  "name": "set",
  "collaborators": [
    "Tim <hello@timothy.best>"
  ],
  "description": null,
  "version": "1.0.0",
  "license": null,
  "repository": null,
  "files": [
    "pkg/set_bg.wasm",
    "pkg/set.d.ts"
  ],
  "main": "pkg/set.js",
  "scripts": {
    "preserve": "cargo build --target wasm32-unknown-unknown --release && wasm-bindgen target/wasm32-unknown-unknown/release/set.wasm --out-dir pkg/ --typescript",
    "serve": "cd demo && webpack-dev-server",
    "pretest": "cargo build --target wasm32-unknown-unknown --release && wasm-bindgen target/wasm32-unknown-unknown/release/set.wasm --out-dir tests/build/ --nodejs",
    "test": "cd tests && yarn install && yarn test"
  }
}
